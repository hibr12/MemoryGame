<<<<<<< HEAD
# MemoryGame
=======
# Memory Game (Card Matching)

Open `index.html` in your browser to play.

Features
- Flip cards to find matching pairs
- Move and match counters
- Restart and win notification

Quick start (from project folder):

Python 3 simple server:
````markdown
# Memory Game (Card Matching)

Open `index.html` in your browser to play.

Features
- Flip cards to find matching pairs
- Move and match counters
- Restart and win notification

Quick start (from project folder):

Python 3 simple server:
```bash
python -m http.server 8000
# then open http://localhost:8000
```

Or open `index.html` directly in the browser.

````
